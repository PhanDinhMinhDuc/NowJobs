namespace WebJob.Models.Enum
{
    public enum VerificationStatus
    {
        Pending = 0,    // Đang chờ xác nhận
        Approved = 1,    // Đã xác nhận
        Rejected = 2     // Từ chối
    }
}